"""
len() gives the length of the string that you give it.
sth.count("sth")
round(number , a)
list.append("item that you want to add the list")
random is a module that must be imported when you wanted to use it.
random.seed() function sets the value of randint function constant according to the seed that you gave.
random.randint(a,b) gives you a random integer between a and b, both inclusive.
use a function from another module, say x, you have to use that function as x.function().
switch a string to next line, use \n in that string.
escape a character in your function, use \. For example, print("kürşat \"camir\"") gives kürşat "camir".
You can split an input or any string (str) by any character you want with using str_string.split(", ").
	To illustrate, that splits the given input "Orhan, Kürşat, Camir" as ['Orhan', 'Kürşat', 'Camir'], namely, as a list.
print("Name", "Is", "Sachin", sep = "**") is a way to separate directly via print.
print("Multiplication is:", res) is a way to represent a result in print.

"""
