"""

You can create a class level attribute by defining it in the class definition.
You can create an instance level attribute by defining it in the __init__ method.
You can use the class level attribute to store data that is shared among all instances of the class.
You can use the instance level attribute to store data that is specific to each instance of the class.
You can use @classmethod to define a class level method.
You can create an instance level method by defining it in the __init__ method.
You can use @staticmethod to define a static method.
A static method works as if it is a regular function, but it is invoked without an instance of the class as the first argument.
__repr__(self) can return an instance or attribute of the class in order to print it as it is.
CSV stands for 'Comma Separated Values'.
You can use the CSV module to read and write CSV files.
You can import csv in order to use the CSV module.
isinstance(a, type) can check if an object, namely "a", is an instance of a class.

"""
